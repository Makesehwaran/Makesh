#include<stdio.h>
#include<conio.h>
void main()
{
char ch;
printf("enter the letter:"ch);
scanf("%d",ch);
{
if((ch>65 && ch <=90)||(ch>=92 && ch <=122))
printf("%c is a not alphabet",ch);
return 0;
}
