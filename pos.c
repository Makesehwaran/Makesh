#include<stdio.h>
#include<conio.h>
void main()
{
int num;
printf("enter the number;",num);
scanf("%d,&num);
if (num>0)
printf("%d is a positive num",num);
else if (num<0)
printf("%d is the negative num",num);
else
printf("%d is zero:,num);
return 0;
}
